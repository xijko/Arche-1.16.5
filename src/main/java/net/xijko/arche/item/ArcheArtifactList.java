package net.xijko.arche.item;

import net.minecraft.item.Item;
import net.minecraftforge.fml.RegistryObject;

public class ArcheArtifactList {




    /*
    public static void serializeArtifactList(){
        String artifacts = "";
        for (RegistryObject<Item> i :ITEMS.getEntries()
        ) {
            if(i.get().getClass().isInstance(ArcheArtifactItem.class)){
                int slot = ((ArcheArtifactItem) i.get()).slot;
                artifactItemsList[slot] = (ArcheArtifactItem) i.get();
                artifacts += i.get().toString();
            }
        }
        LOGGER.warn("LIST OF ARTIFACTS: "+artifacts);

    }
     */
}
